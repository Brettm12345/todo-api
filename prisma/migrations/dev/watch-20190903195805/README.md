# Migration `watch-20190903195805`

This migration has been generated by Brett Mandler <brettmandler@gmail.com> at 9/3/2019, 7:58:05 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Todo"("author" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,"content" TEXT    ,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,"done" BOOLEAN NOT NULL DEFAULT false  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,"title" TEXT NOT NULL DEFAULT ''  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  )
;

CREATE TABLE "lift"."User"("createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,"email" TEXT NOT NULL DEFAULT ''  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,"name" TEXT NOT NULL DEFAULT ''  )
;

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190903195805
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+datasource sqlite {
+  url      = "file:data.db"
+  provider = "sqlite"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+  output   = "../node_modules/@generated/nexus-prisma"
+}
+
+generator photon {
+  provider = "photonjs"
+  output   = "../node_modules/@generated/photon"
+}
+
+model Todo {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  title     String
+  content   String?
+  done      Boolean  @default(false)
+  author    User
+}
+
+model User {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  email     String   @unique
+  name      String
+  todos     Todo[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190903195805)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190903195805'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
